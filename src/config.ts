const config = {
  // Devaloper Number
  devUser: [""],
  devGrup: [""],

  // Prefix for commands
  prefix: "tod!",

  // Axios instance url
  baseURL: "https://komikcast-api.vercel.app",
};

export default config;
